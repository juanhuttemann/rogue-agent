[project]
name = "rogue-agent"
version = "0.1.0"
description = "Crescendo attack demo: a benign agent steered into destructive code, contained only by a sandbox"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "agent-framework>=1.11.0",
    "opensandbox>=0.1.15",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
]
